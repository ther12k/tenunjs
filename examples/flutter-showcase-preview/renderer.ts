/**
 * The showcase preview intentionally uses the same Canvas 2D renderer and
 * fixed-overlay/display-list contract as the gallery preview.
 */
export {
  CanvasPreviewRenderer,
  anchorOffsetForTest,
} from "../gallery-preview/renderer";
export type { PreviewRenderer } from "../gallery-preview/renderer";
